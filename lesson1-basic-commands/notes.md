# Lesson 1 – Basic Commands

## Commands
- `whoami` – shows current user
- `pwd` – shows current directory
- `ls` – list files and folders
- `ls -l` – long list with permissions
- `clear` – clears screen
- `uname -a` – system info

These are the foundation of working in any Linux terminal.
