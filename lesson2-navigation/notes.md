# Lesson 2 – Navigating Linux Filesystem

Commands:
- `cd` – change directory
- `cd /` – go to root
- `cd ~` – home directory
- `cd ..` – parent directory
- `cd .` – current directory

Concepts:
- Absolute paths start with `/`
- Relative paths depend on current directory
